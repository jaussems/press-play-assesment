export interface IinputComponent {
  placeholder: string;
  width: string;
  class?: string;
  icon: boolean;
}
export interface IButton {
  text: string;
  isLight?: string;
}

export interface IArticle {
  src: string;
  alt: string;
  header: string;
  paragraph: string;
  class?: string;
  buttonText: string;
  isLightBtn?: boolean;
}
